import { Routes } from '@angular/router';
import { ComplexityComponent } from '../app/complexity/complexity.component';


const routes: Routes = [
    {
        path: 'complexity',
        component: ComplexityComponent
    }
];

export default routes; 
